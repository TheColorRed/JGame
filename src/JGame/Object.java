package JGame;

import JGame.Components.Collider;

public class Object{

    // Runs on game creation for each object
    public void start(){

    }

    // Runs on every frame for each object
    public void update(){

    }

    // Runs on every tick for each object
    public void fixedUpdate(){

    }

    // Runs when the object collides with another object
    // (Both objects must have colliders on them)
    public void onCollision(){

    }

    // Runs when the object collides with another object
    // (Both objects must have colliders on them)
    public void onCollision(Collider other){

    }
}
